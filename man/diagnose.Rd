% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{diagnose}
\alias{diagnose}
\alias{diagnose.survatr_fit}
\title{Diagnose a fitted survatr hazard model}
\usage{
diagnose(fit, ...)

\method{diagnose}{survatr_fit}(fit, ...)
}
\arguments{
\item{fit}{A \code{survatr_fit} returned by \code{surv_fit()}.}

\item{...}{Unused; reserved for future arguments.}
}
\value{
A \code{survatr_diag} object: a named list with elements \code{positivity},
\code{balance}, \code{weights}, \code{censoring}, \code{competing}. Each is a \code{data.table}
indexed by \code{time} (and \code{cause} for \code{competing}), or \code{NULL} when the panel
does not apply.
}
\description{
Returns per-period diagnostic panels for a \code{survatr_fit}:
\describe{
\item{positivity}{Predicted hazard distribution by time; flags periods
with hazards near 0 or 1.}
\item{balance}{Standardized mean differences (or treatment–confounder
correlation for continuous treatment) by time.}
\item{weights}{IPW only: per-id weight summary (ESS, max, top-5\% share).
\code{NULL} for gcomp / ice.}
\item{censoring}{Per-period censoring incidence by arm. \code{NULL} when no
censoring column was supplied.}
\item{competing}{Competing risks only: per-cause CIF + all-cause survival
+ identity check \verb{Σ F^(j)(t) + S(t) = 1}. \code{NULL} for single-event fits.}
}
}
\details{
Most panels operate on the at-risk rows from \code{build_risk_set()} (the rows
the hazard model was fit on). \strong{Exception:} \verb{$censoring} uses the full
person-period grid so that the censoring event itself is visible at the
period it occurs; its \code{n_pp_rows} denominator therefore includes post-event
rows and differs from \verb{$positivity$n_at_risk}. For Track B (ICE) the hazard
model is fit lazily inside \code{contrast()}, so the positivity panel reports
the empirical per-period event rate rather than model-predicted hazards.
}
\examples{
set.seed(3)
n_id <- 50L; K <- 4L
pp <- data.frame(
  id   = rep(seq_len(n_id), each = K),
  t    = rep(seq_len(K), times = n_id),
  A    = rep(rbinom(n_id, 1L, 0.5), each = K),
  L    = rep(rnorm(n_id), each = K),
  Y    = rbinom(n_id * K, 1L, 0.05)
)
fit <- surv_fit(pp, "Y", "A", ~L, "id", "t", time_formula = ~1)
dx  <- diagnose(fit)
print(dx)
}
\seealso{
\code{\link[=surv_fit]{surv_fit()}}, \code{\link[=contrast]{contrast()}}

Other survatr_fit functions: 
\code{\link{print.survatr_diag}()},
\code{\link{print.survatr_fit}()},
\code{\link{surv_fit}()}
}
\concept{survatr_fit functions}
