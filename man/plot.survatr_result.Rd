% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.survatr_result}
\alias{plot.survatr_result}
\title{Plot a \code{survatr_result}}
\usage{
\method{plot}{survatr_result}(
  x,
  which = c("auto", "curves", "contrasts"),
  main = NULL,
  xlab = NULL,
  ylab = NULL,
  col = NULL,
  ribbon = TRUE,
  ribbon_alpha = 0.2,
  ...
)
}
\arguments{
\item{x}{A \code{survatr_result}.}

\item{which}{One of \code{"auto"}, \code{"curves"}, \code{"contrasts"}. \code{"auto"}
picks \code{curves} for \code{survival} / \code{risk} / \code{rmst} and \code{contrasts} for
the three pairwise types.}

\item{main}{Optional plot title.}

\item{xlab, ylab}{Optional axis labels. Sensible defaults are picked
based on \code{type}.}

\item{col}{Optional vector of colors, length \code{n_interventions} (or
\code{n_contrasts}). Defaults to \code{grDevices::palette()}.}

\item{ribbon}{Logical; when \code{TRUE} and CI columns are populated, shade
a polygon between \code{ci_lower} and \code{ci_upper}. Default \code{TRUE}.}

\item{ribbon_alpha}{Numeric in \verb{(0, 1)}; ribbon transparency (via
\code{grDevices::adjustcolor}). Default \code{0.2}.}

\item{...}{Passed to the underlying \code{plot.default()} call.}
}
\value{
The \code{survatr_result}, invisibly.
}
\description{
Base-R graphics rendering of survival / risk / RMST curves or of
risk-difference / risk-ratio / RMST-difference contrasts. Dispatches
on \code{x$type}:
}
\details{
\itemize{
\item \code{survival} / \code{risk} / \code{rmst}: one line per intervention (colored),
CI ribbons when populated.
\item \code{risk_difference} / \code{risk_ratio} / \code{rmst_difference}: one line per
contrast (colored), CI ribbons when populated, and a reference line
at 0 (for differences) or 1 (for ratios).
}
}
