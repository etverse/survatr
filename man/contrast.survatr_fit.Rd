% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrast.R
\name{contrast.survatr_fit}
\alias{contrast.survatr_fit}
\title{Survival-curve contrasts on a fitted hazard model}
\usage{
\method{contrast}{survatr_fit}(
  fit,
  interventions,
  times,
  type = NULL,
  cause = NULL,
  q = 0.5,
  reference = NULL,
  ci_method = "none",
  conf_level = 0.95,
  n_boot = 500L,
  boot_ci = "percentile",
  parallel = "no",
  ncpus = 1L,
  seed = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{survatr_fit}.}

\item{interventions}{A named list of \code{causatr_intervention} objects
(\code{causatr::static()}, \code{causatr::shift()}, \code{causatr::scale_by()},
\code{causatr::threshold()}, \code{causatr::dynamic()}). The element names are
carried through to the \code{intervention} column of the result.}

\item{times}{Numeric vector of time points at which to evaluate the
survival curve. Must all be elements of \code{fit$time_grid} — extrapolation
beyond the observed grid is rejected with
\code{survatr_time_extrapolation}.}

\item{type}{Estimand. For a single-event fit: one of \code{"survival"},
\code{"risk"}, \code{"risk_difference"}, \code{"risk_ratio"}, \code{"rmst"},
\code{"rmst_difference"}, \code{"rmtl"} (restricted mean time lost, \code{t - RMST}),
\code{"rmtl_difference"}, or \code{"quantile"} (survival-time quantile / median;
default \code{"risk_difference"}). For a competing-risks fit
(\code{surv_fit(..., competing = )}): one of \code{"cif"} (per-cause cumulative
incidence), \code{"cif_difference"}, \code{"cif_ratio"} (default), \code{"yll"} (per-cause
years of life lost, \verb{int F^(j)}), all-cause \code{"survival"} / \code{"risk"} /
\code{"rmst"} / \code{"rmtl"}, or all-cause \code{"quantile"} (from the summed
cause-specific hazards). Mixing a CIF estimand with a single-event fit (or
vice versa) aborts with \code{survatr_competing_type}; \code{"yll"} on a single-event
fit aborts with \code{survatr_yll_needs_cr}.}

\item{cause}{Competing-risks only. Integer vector selecting which cause(s)
to report for the \code{cif} estimands, or \code{NULL} (the default) for all causes.
Validated against the fitted causes; ignored for \code{survival} / \code{risk} and
for single-event fits.}

\item{q}{For \code{type = "quantile"}, the quantile level(s) of the survival-time
distribution to report -- a numeric vector with every entry in \verb{(0, 1)}
(default \code{0.5} = median). The result is indexed by \code{q} (one \code{tau_hat} per
\verb{(intervention, q)}) rather than by \code{time}. Aborts with
\code{survatr_quantile_unreached} when the curve never crosses \code{1 - q} on the
grid, and \code{survatr_bad_q} for out-of-range \code{q}. Ignored for other types.}

\item{reference}{Name of the intervention used as the reference in
difference / ratio contrasts. Defaults to the first name in
\code{interventions}. Ignored by \code{type = "survival"}, \code{"risk"}, \code{"rmst"}, and
\code{"cif"} (no pairwise contrast).}

\item{ci_method}{One of \code{"none"} (point estimates only), \code{"sandwich"}
(delta-method cross-time IF aggregation via
\code{causatr:::prepare_model_if()}), or \code{"bootstrap"} (resample
individuals, refit the hazard model per replicate, percentile or
Wald CIs). Default \code{"none"}.}

\item{conf_level}{Confidence level for the CIs when \code{ci_method != "none"}.
Numeric scalar in \verb{(0, 1)}, default \code{0.95}.}

\item{n_boot}{Integer; number of bootstrap replicates. Ignored when
\code{ci_method != "bootstrap"}. Default \code{500L}.}

\item{boot_ci}{One of \code{"percentile"} (sample-quantile CI) or \code{"wald"}
(point estimate +/- \code{z * sd(replicates)}). Default \code{"percentile"}.
Percentile is transform-invariant and is the safer default for
ratios / RMST.}

\item{parallel}{One of \code{"no"}, \code{"multicore"}, \code{"snow"}; forwarded to
\code{boot::boot()}. Default \code{"no"}.}

\item{ncpus}{Integer; number of CPUs for parallel bootstrap. Default
\code{1L}.}

\item{seed}{Integer scalar or \code{NULL}. When non-null, \code{set.seed(seed)}
is called before the bootstrap loop so the replicate sequence is
reproducible. Default \code{NULL}.}

\item{...}{Unused; reserved for future chunks.}
}
\value{
A \code{survatr_result} list with \code{estimates}, \code{contrasts},
\code{time_grid}, \code{type}, \code{reference}, \code{ci_method}, and \code{call} components.
}
\description{
Given a \code{survatr_fit} (chunk 1) and a named list of interventions, build
the counterfactual person-period data under each intervention, predict
the per-row hazard, cumulate within individual to get \code{S^a_i(t)}, and
average across individuals to get the population survival curve
\verb{S^a(t) = (1/n) sum_i S^a_i(t)}. Derive risk / risk-difference /
risk-ratio / RMST / RMST-difference contrasts from there.

\strong{No variance in this chunk.} All \code{se} / \verb{ci_*} columns come back as
\code{NA_real_}; sandwich variance ships with chunk 3 (delta-method cross-time
IF aggregation) and bootstrap with chunk 4.
}
\examples{
# Small rectangular person-period dataset: 40 ids over 5 periods.
set.seed(2)
n_id <- 40L
K <- 5L
pp <- data.frame(
  id = rep(seq_len(n_id), each = K),
  t = rep(seq_len(K), times = n_id),
  A = rep(rbinom(n_id, 1L, 0.5), each = K),
  Y = rbinom(n_id * K, 1L, 0.1)
)

fit <- surv_fit(pp, "Y", "A", ~1, "id", "t", time_formula = ~ factor(t))

ivs <- list(a1 = causatr::static(1), a0 = causatr::static(0))

# Point-estimate survival curves under each intervention.
contrast(fit, interventions = ivs, times = 1:5, type = "survival")

# Risk difference with delta-method sandwich CIs.
contrast(
  fit,
  interventions = ivs,
  times = 1:5,
  type = "risk_difference",
  ci_method = "sandwich"
)
}
\seealso{
Other survatr_result methods: 
\code{\link{contrast}()},
\code{\link{forrest}()},
\code{\link{forrest.survatr_result}()},
\code{\link{plot.survatr_result}()},
\code{\link{print.survatr_result}()},
\code{\link{tidy.survatr_result}()}
}
\concept{survatr_result methods}
