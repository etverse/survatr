% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_fit.R
\name{surv_fit}
\alias{surv_fit}
\title{Fit a causal survival hazard model on person-period data}
\usage{
surv_fit(
  data,
  outcome,
  treatment,
  confounders,
  id,
  time,
  censoring = NULL,
  competing = NULL,
  time_formula = ~splines::ns(time, 4),
  weights = NULL,
  estimator = "gcomp",
  model_fn = stats::glm,
  propensity_model_fn = stats::glm,
  trim = NULL,
  confounders_tv = NULL,
  history = Inf,
  ...
)
}
\arguments{
\item{data}{A person-period (long) \code{data.frame} or \code{data.table},
\strong{rectangular} across ids: every unique \code{id} must have one row at
every unique \code{time} value. Ragged PP (ids dropped post-event /
post-censor) is rejected with class \code{survatr_ragged_pp}. Pad ragged
data before calling \code{surv_fit()} by appending rows with
\code{outcome = 0} and (if used) \code{censoring = 1} so the risk-set builder
drops them from the fit. Wide data (one row per id across a
multi-period study) must be reshaped with
\code{causatr::to_person_period()}.}

\item{outcome}{Character scalar. Column name of the event indicator
(\code{1} = event at this period, \code{0} = no event). Must be in \code{data}.}

\item{treatment}{Character scalar. Column name of the treatment. For Track A
the treatment is constant within \code{id}. Under \code{estimator = "ice"} (Track B)
the treatment may vary within \code{id} and must be \strong{numeric} (binary, or a
numeric dose entered linearly); factor / categorical (k > 2) treatments are
rejected with class \code{survatr_ice_treatment_unsupported} (a treatment-design
formula path ships in a later chunk).}

\item{confounders}{A one-sided formula (e.g. \code{~ L1 + L2}) describing the
\strong{baseline} (time-invariant) covariate adjustment set. Under
\code{estimator = "ice"} (Track B), time-varying covariates go in
\code{confounders_tv} instead; baseline terms here are never lagged.}

\item{id}{Character scalar. Column name of the individual identifier.}

\item{time}{Character scalar. Column name of the discrete period index
(integer-valued; sorted within \code{id}).}

\item{censoring}{Character scalar or \code{NULL}. Column name of the censoring
indicator (\code{1} = censored at this period). \code{NA} or \code{0} means uncensored.
When \code{NULL}, every uncensored period is treated as at-risk until the
first event.}

\item{competing}{Character scalar or \code{NULL} (the default). When non-\code{NULL},
activates the \strong{competing-risks} path: \code{competing} names a multi-valued
event-type column (\code{0} = no event this period, \verb{1..J} = the cause of the
event this period), and \code{surv_fit()} fits \code{J} parallel cause-specific
pooled-logistic hazard models on a shared all-cause risk set. It must name
the \strong{same} column as \code{outcome}; competing risks are gcomp / Track A only
in this release (a non-\code{"gcomp"} estimator, fewer than two distinct causes,
or \code{outcome != competing} aborts with \code{survatr_competing_estimator} /
\code{survatr_competing_misuse}). Cumulative-incidence functions (CIF), CIF
contrasts, and all-cause survival come from \code{contrast()}. Fine--Gray /
subdistribution hazards are out of scope (cause-specific only).}

\item{time_formula}{One-sided formula for the baseline hazard
\code{alpha(t)}. Defaults to \code{~ splines::ns(time, 4)} (4 df natural spline
on the time variable). Pass \code{~ factor(time)} for period dummies or
\code{~ 1} for a time-constant hazard.}

\item{weights}{Optional numeric vector of external weights, length
\code{nrow(data)}. When supplied, the hazard model is fit with
\code{stats::quasibinomial()} rather than \code{stats::binomial()} (same score
equations, free dispersion -- drops the "non-integer #successes"
warning). The variance engine in later chunks reads the family from
\code{fit$family} to pick the right dispersion.}

\item{estimator}{Character scalar. \code{"gcomp"} (pooled-logistic
standardization), \code{"ipw"} (weighted marginal hazard MSM with stabilized
density-ratio weights), or \code{"ice"} (Track B: longitudinal
iterated-conditional-expectation hazards for a time-varying treatment).
Matching is a hard reject with class \code{survatr_matching_rejected}
pointing to \code{survival::coxph(..., weights = , cluster = )}.}

\item{model_fn}{Fitting function for the hazard model. Defaults to
\code{stats::glm}. Accepts any function matching the \code{stats::glm} interface
(formula, data, family, weights, ...), e.g. \code{mgcv::gam} with an
\code{s(time)} term in \code{time_formula}.}

\item{propensity_model_fn}{Fitting function for the \strong{treatment} model
under \code{estimator = "ipw"} (ignored otherwise). Same \code{stats::glm}-style
interface as \code{model_fn}. Defaults to \code{stats::glm}. The treatment model is
fit on the baseline rows (one per id) with \code{confounders} as predictors;
the hazard MSM then uses \code{A} only.}

\item{trim}{Numeric scalar in \verb{(0, 1]} or \code{NULL} (the default). Under
\code{estimator = "ipw"}, the per-id stabilized weights are winsorized at the
\code{trim}-th quantile (Cole & Hernán 2008) before being broadcast onto the
person-period rows. \code{NULL} / \code{1} means no truncation. The resolved fixed
cutoff is reused by the sandwich variance.}

\item{confounders_tv}{A one-sided formula of \strong{time-varying} confounders
for Track B (\code{estimator = "ice"}), lag-expanded at each backward step
(e.g. \code{~ L} builds \code{L + lag1_L + ...}). \code{NULL} (the default) means no
time-varying confounders. Ignored by Track A (\code{gcomp} / \code{ipw}).}

\item{history}{Markov lag order for Track B. \code{Inf} (the default) uses the
full available history (capped at \code{n_times - 1}); an integer restricts
the lag structure (e.g. \code{history = 1} for first-order Markov). Ignored by
Track A.}

\item{...}{Forwarded to \code{model_fn}. \code{na.action = na.exclude} is rejected
with class \code{survatr_bad_na_action} -- \code{na.exclude} pads working
residuals with \code{NA}s while \code{model.matrix()} drops them, which silently
corrupts the sandwich variance downstream.}
}
\value{
An object of class \code{survatr_fit} holding the fitted hazard model,
the person-period data (internal \verb{.survatr_*} columns stripped), the
time grid, and metadata needed by \code{contrast()} and \code{diagnose()}.
}
\description{
Fit-only entry point for survatr. Builds the risk set and fits the
pooled-logistic discrete-time hazard model
\verb{logit h(t | A, L) = alpha(t) + beta_A A + beta_L L} on the at-risk
person-period rows. Survival curves, risk / RMST contrasts, and variance
live in \code{contrast()} (time-indexed curve-shaped result). This two-step
split lets the user fit the hazard model once and cheaply contrast many
interventions on top.
}
\examples{
# Small rectangular person-period dataset: 30 ids over 4 periods.
set.seed(1)
n_id <- 30L
K <- 4L
pp <- data.frame(
  id = rep(seq_len(n_id), each = K),
  t = rep(seq_len(K), times = n_id),
  A = rep(rbinom(n_id, 1L, 0.5), each = K),
  Y = rbinom(n_id * K, 1L, 0.1)
)

# Pooled-logistic hazard with period dummies for the baseline hazard.
fit <- surv_fit(pp, "Y", "A", ~1, "id", "t", time_formula = ~ factor(t))
fit
}
\seealso{
\code{causatr::to_person_period()} for reshaping wide data.

Other survatr_fit functions: 
\code{\link{diagnose}()},
\code{\link{print.survatr_diag}()},
\code{\link{print.survatr_fit}()}
}
\concept{survatr_fit functions}
