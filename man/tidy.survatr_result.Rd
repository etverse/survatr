% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.survatr_result}
\alias{tidy.survatr_result}
\title{Tidy a \code{survatr_result} into long format}
\usage{
\method{tidy}{survatr_result}(x, which = c("all", "estimates", "contrasts"), conf.int = TRUE, ...)
}
\arguments{
\item{x}{A \code{survatr_result} from \code{contrast()}.}

\item{which}{One of \code{"estimates"} (per-intervention rows only),
\code{"contrasts"} (pairwise rows only), or \code{"all"} (both stacked).
Default \code{"all"}.}

\item{conf.int}{Logical; when \code{FALSE}, drop \code{ci_lower} / \code{ci_upper}
columns. Default \code{TRUE}.}

\item{...}{Unused.}
}
\value{
A \code{data.frame} in long format with columns
\code{intervention | contrast | time | estimand | estimate | se | ci_lower | ci_upper} (or a subset when \code{which} / \code{conf.int}
restrict it).
}
\description{
Stacks the per-intervention \code{estimates} and pairwise \code{contrasts} into a
single long \code{data.frame} suitable for downstream plotting or further
aggregation. The \code{estimand} column identifies which column the row
came from: \code{s_hat} / \code{risk_hat} / \code{rmst_hat} for intervention rows,
\code{risk_difference} / \code{risk_ratio} / \code{rmst_difference} for contrast rows.
}
\examples{
set.seed(2)
n_id <- 40L
K <- 5L
pp <- data.frame(
  id = rep(seq_len(n_id), each = K),
  t = rep(seq_len(K), times = n_id),
  A = rep(rbinom(n_id, 1L, 0.5), each = K),
  Y = rbinom(n_id * K, 1L, 0.1)
)
fit <- surv_fit(pp, "Y", "A", ~1, "id", "t", time_formula = ~ factor(t))
res <- contrast(
  fit,
  interventions = list(a1 = causatr::static(1), a0 = causatr::static(0)),
  times = 1:5,
  type = "risk_difference"
)
head(tidy(res))
}
\seealso{
Other survatr_result methods: 
\code{\link{contrast}()},
\code{\link{contrast.survatr_fit}()},
\code{\link{forrest}()},
\code{\link{forrest.survatr_result}()},
\code{\link{plot.survatr_result}()},
\code{\link{print.survatr_result}()}
}
\concept{survatr_result methods}
