% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print.survatr_diag}
\alias{print.survatr_diag}
\title{Print a \code{survatr_diag}}
\usage{
\method{print}{survatr_diag}(x, ...)
}
\arguments{
\item{x}{A \code{survatr_diag}.}

\item{...}{Unused.}
}
\value{
The diagnostic object, invisibly.
}
\description{
Compact per-panel summary for the diagnostic object returned by
\code{diagnose()}. Reports how many periods have positivity flags, the range
of SMDs in the balance panel, IPW weight summary (if applicable), and the
competing-risks identity-check result (if applicable).
}
\examples{
set.seed(4)
n_id <- 40L; K <- 3L
pp <- data.frame(
  id = rep(seq_len(n_id), each = K),
  t  = rep(seq_len(K), times = n_id),
  A  = rep(rbinom(n_id, 1L, 0.5), each = K),
  L  = rep(rnorm(n_id), each = K),
  Y  = rbinom(n_id * K, 1L, 0.05)
)
fit <- surv_fit(pp, "Y", "A", ~L, "id", "t", time_formula = ~1)
print(diagnose(fit))
}
\seealso{
Other survatr_fit functions: 
\code{\link{diagnose}()},
\code{\link{print.survatr_fit}()},
\code{\link{surv_fit}()}
}
\concept{survatr_fit functions}
